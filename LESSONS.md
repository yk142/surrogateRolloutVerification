# M1-M16で得られた教訓

振り子の物理モデルをNeural State Space (NSS) モデルでサロゲート化し、ロールアウト誤差・
制御タスクでの実用性を検証してきたプロジェクトの、マイルストーンごとの要約と横断的な教訓。
各マイルストーンの詳細は対応するGitHub Issueを参照。

## マイルストーン要約

### M1: 単振り子ロールアウト誤差検証(#1)
単振り子(無減衰・無入力)でNSS状態遷移モデル(MLP、`(sinθ, cosθ, θ̇)`エンコード)を
1-step教師強制損失で学習。ロールアウト誤差は学習ホライズン(1秒)を過ぎると加速度的に
蓄積し、約4秒で飽和。サロゲートはエネルギーを単調に注入し続ける非保存的な誤差を持ち、
倒立近傍・完全回転域(高エネルギー域)は一様IC分布では過小表現され、ロールアウトが
大きく破綻することが判明した。

### M2: 減衰項とIC分布の層化(#3)
減衰項 `c` を追加し、IC分布を3層(normal / rotation / inverted)に層化サンプリング。
M1最大の弱点だった倒立近傍・完全回転域の精度が大幅改善(最終RMSE 2.68→2.37)。
一方、完全回転域で「2周目以降の減衰が速すぎる」・小振幅域で「減衰不足でわずかに発散する」
という逆方向のバイアスが両方観測された。

**教訓**: IC分布の偏りは、サロゲートの精度が悪い領域を直接生み出す。層化サンプリングで
対象領域を明示的に増やすことは効果的だが、副作用として別領域にバイアスが移る可能性がある。

### M3: トルク入力の導入(#5)
状態遷移にトルク入力 `τ`(ZOH)を追加し、`x_{t+1}=f_θ(x_t,u_t)` を学習。同一モデル容量では
制御入力を加えたことでタスク難度が上がり、長時間ロールアウトのRMSEはM2単体条件より悪化した
(2.37→3.5〜3.8)。モデル容量を2倍にしても改善せず。一方、エネルギー推移や共振トルクによる
スイングアップの定性的挙動は良く再現できた。

**教訓**: ポイントワイズなロールアウト誤差(RMSE)の悪化は、位相・タイミングのズレによる
ものが大きく、定性的な物理挙動の再現度とは別の話。「長時間の完全開ループ予測より、
短ホライズン予測+頻繁な再計画(MPC)の方が実用的」という設計方針がここで得られた。

### M4: スイングアップMPC(#7)
正弦波パラメータ化したランダムシューティングMPCを実装。実プラントは常に真の物理モデルで
ステップし、予測モデル(NSSサロゲート/真の物理モデル)だけを差し替えて比較。オラクルMPCは
ほぼ完全にθ=πへ収束(誤差0.0065rad)。NSSサロゲートMPCもスイングアップ・大まかな安定化は
達成するが、系統的なオフセット(平均角距離0.27rad、5試行ともstd0.006の低分散=再現性のある
バイアス)が残った。フィードバックなしのベースライン(開ループ共振トルク)は倒立近傍に
全く留まれず、MPC(receding horizon)の効果が明確だった。

**教訓**: M3の設計方針(短ホライズン+頻繁な再計画)は実際に機能し、サロゲートでもMPCの
枠組みに組み込めば実用的なスイングアップ制御ができる。ただし系統バイアスは残る。

### M5: PID安定化(#9)
倒立目標のPIDコントローラを実装し、同一ゲインを真の物理モデル/NSSサロゲートに閉ループ
適用して比較。小さな偏差では両者とも良く一致して安定化する(成功: 真値12/18、サロゲート
15/18)が、訓練データがカバーする範囲の境界〜外側の大きな偏差(角速度±1rad/s以上)では、
**真の物理モデルはトルク飽和により安定化に失敗する一方、サロゲートは誤って成功して
しまう**という逆方向の乖離が見つかった。

**教訓**: サロゲートは訓練データの範囲外で「実際より安定化しやすい」という楽観的な
バイアスを持ちうる。サロゲート単体の評価では気づけず、真値との比較で初めて判明した。

### M6: PTP制御(#11)
PIDを目標角度可変に一般化(重力フィードフォワード `mgL*sin(target)` を追加。倒立(pi)
以外の目標では、これがないと定常重力トルクを補償できず転落するバグを発見・修正)。
倒立近傍の小さなPTP(±0.2rad程度)は真値・サロゲートともに良好に追従する一方、単発
ステップ応答では**オフセット方向によって非対称な失敗パターン**が見られた(真値: 対称的に
±0.4radで失敗。サロゲート: +0.3rad以降で失敗するが-0.4radはむしろ成功)。この非対称性は
M4で見つかった系統的オフセットバイアスと方向的に整合していた。

**教訓**: サロゲートの系統バイアスは複数の制御タスク(MPC・PID・PTP)を横断して一貫した
方向性を持って現れることがあり、一つのタスクで見つかったバイアスが他のタスクの挙動を
予測する手がかりになる。

### M7: マルチステップ(ロールアウト)損失(#15)
1-step教師強制損失を、Kステップ自己回帰展開してから損失を取る「マルチステップ損失」に
変更。**いきなり大きなK(K=10や30)で最初から学習すると、学習損失は下がってもロールアウト
評価(RMSE)はむしろ1-step版より悪化する**ことが判明。K=1→3→5→10→20→30と段階的に
伸ばすカリキュラム学習(+勾配クリッピング)を導入して初めて安定して学習できた。

M3と同じ3条件評価でBefore/After比較した結果は**混合**: ゼロトルク(3.83→3.43)・共振
トルク(3.52→3.39)は改善したが、ランダムトルク(3.64→4.92)は悪化。系統バイアスの
「向き」自体も状態領域によって一貫せず、より複雑な誤差構造になった。

**教訓**: マルチステップ損失は「1-step損失とロールアウト時挙動のミスマッチ」を狙った
改善だが、(1) 素朴な実装では訓練が不安定化しうる(カリキュラム学習が実質必須)、
(2) 改善は均一ではなく条件によってトレードオフがある、という2点が重要な知見。

### M8: M7モデルでのM5(PID)再検証(#17)
M7でリトレインしたモデルに対し、コード変更なしでM5と同じPID安定化評価を再実行。
成功数が真値12/18(不変)・サロゲート15/18→13/18に低下。M5では成功していた
`Δθ0=-0.1, θ̇0=-1.0` がM7モデルでは大きく失敗する**新規のリグレッション**が見つかった
一方、他の複数ICでは逆に改善していた。

### M9: M7モデルでのM4(MPC)・M6(PTP)再検証(#19)
同様にM4・M6もM7モデルで再検証。
- **M4**: NSSサロゲートMPCの平均安定化誤差が0.269→0.541に悪化しただけでなく、
  ばらつきがstd0.006→0.442に激増(試行によって0.06〜1.33と大きくばらつく不安定な挙動)。
- **M6**: 単発ステップ応答の失敗方向が完全に反転(+0.3/+0.4側の失敗→-0.3/-0.4側の失敗)。

**教訓(M8・M9共通、プロジェクト最大の発見)**: **ロールアウト誤差(RMSE)という
集計指標の改善は、個別の制御タスクの成功率・安定性の改善を全く保証しない。**
M4では平均・ばらつき両方が悪化、M5では成功数が悪化、M6では失敗方向が反転する
(かつ一部条件では改善する)という、タスクごとに異なる形で表れた。これは
model-based RL文献でいう "objective mismatch problem" の具体的な実例と言える。

### M10: 教訓ドキュメント化(#21)
M1-M9で得られた知見をこの `LESSONS.md` として整理し、以降の方針検討(M7リグレッション
の根本原因調査など)の土台とした。

### M11: マルチステップ損失リグレッションの根本原因調査(#23)
M8/M9のリグレッションの原因を、倒立平衡点(θ=π, θ̇=0, τ=0)での離散時間ヤコビアン比較で
調査。**平衡点そのものでの局所線形化は、1-stepモデル・マルチステップモデルともに真値と
ほぼ同等の精度**(||A-A_true||: 0.0076 vs 0.0091)であり、「局所線形化の歪み」という
仮説は否定された。代わりに、平衡点から大きく外れたIC(θ0=π-0.1, θ̇0=-1.0、最下点通過を
伴う落下→再上昇の軌道)でPID回復挙動を比較したところ、1-stepモデルは8秒後に倒立へ復帰
する(最終誤差0.083)のに対し、マルチステップモデルは再上昇できず最下点付近に留まって
しまう(最終誤差2.398)ことを確認した。

**教訓**: リグレッションの原因は倒立平衡点の局所線形化ではなく、平衡点から大きく外れた
大振幅域(最下点通過を含む落下→再上昇の過程)での動特性の歪みにあった。

### M12: 振幅スイープでの定量比較(#25)
M11の単一事例を一般化するため、θ0=π-0.1から初期角速度|θ̇0|を0.2〜3.0rad/sでスイープし、
PID回復の成否を真値・1-stepモデル・マルチステップモデルで比較。**1-stepモデルは、
トルク飽和のため真値が実際に失敗する中間の角速度域でも常に成功してしまう**(15/15成功、
真値は6/15)という楽観バイアスを、連続的なスイープで劇的に確認した。一方、
**マルチステップモデルの失敗領域(7/15成功)は真値の失敗領域の形状とよく一致**していた。

**教訓**: 「制御タスクの成功率が高い」ことも、良いサロゲートの証明にはならない。
1-stepモデルの「常に成功する」性質は、実際には現実の制約(トルク飽和)を捉え損ねている
という「誤り」であり、評価すべきは成功率そのものではなく**真値の成功/失敗パターンとの
一致度**である。

### M13: オンポリシーデータ収集(DAgger風)による再学習(#27)
現在のサロゲート+PIDを閉ループさせて訪れた状態・トルク系列を、真の物理モデルで
ラベル付けした学習ウィンドウ(DAggerデータ)を収集し、標準の訓練データに混ぜて再学習。
M5の18通りグリッド+M12の15通りスイープ(既知のリグレッション事例)から1254ウィンドウを
収集した結果、M12のスイープでは成功数が7/15→9/15に改善し失敗パターンの形状も真値に
近づいたが、M5のグリッドはほぼ横ばい(13/18→12/18)で、最も直したかった特定の
リグレッション事例は直らなかった。

**教訓**: 1回のDAggerイテレーションでは、狙った特定の失敗事例を確実に修正するには
不十分だった。古典的なDAggerは収集→再学習を複数回反復する手法であり、反復すれば
さらに改善する可能性がある。

### M14: グレーボックス化(既知物理+残差学習)(#29)
既知の物理構造(重力項・トルク入力項・RK4積分)をハードコードし、NNは残差加速度のみを
学習する `GrayBoxNSSModel` を実装。同一学習レシピでの比較:

| 指標 | ブラックボックス | グレーボックス |
|---|---|---|
| 開ループRMSE (ゼロトルク, t=6s) | 3.43 | **0.17** |
| 開ループRMSE (ランダムトルク) | 4.92 | **0.23** |
| 開ループRMSE (共振トルク) | 3.39 | **0.54** |
| MPC安定化誤差 平均 (オラクル=0.0065) | 0.269〜0.541 | **0.0091** |
| M12スイープで真値の成功/失敗パターンと一致 | 10/15 | **14/15** |
| M5グリッドで真値の成功/失敗パターンと一致 | 15/18 | **17/18** |

構造的に得られた副次的な利点:
- 積分構造が正しいためエネルギー挙動が構造的に妥当になる(M1のエネルギー注入が消える)
- トルクが `tau/(mL^2)` として厳密に入るため、トルク飽和の帰結を正しく予測できる
  (M5/M12の楽観バイアスが消える)
- theta を atan2 で毎ステップ畳み込まないため、出力が連続になり、M5/M6で悩まされた
  プロット上のブランチカット由来のジャンプも構造的に発生しない

**留保**: このM14時点では、**グレーボックスと真のモデルの差は減衰項 `-c·θ̇` のみ**で
あり、NNに残された仕事は「1次元の線形関数を当てる」ことだけだった(学習された残差は
実測で全点 `-0.15·θ̇` と誤差±0.007以内)。M14の桁違いの改善は、学習問題をほぼ自明に
した結果という自己循環的な側面が大きく、この留保を解消するためM15・M16を行った。

**教訓**: 学習方法・データの工夫より、モデル構造(既知物理の埋め込み)の方が桁違いに効く。
M1-M13で積み上げた学習方法・データ側の工夫(層化サンプリング、マルチステップ損失、
カリキュラム学習、DAgger)による改善はいずれも数十%程度の部分的なものだったが、
グレーボックス化は一度に桁違いの改善をもたらし、それまで繰り返し現れた失敗モード
(エネルギー注入、楽観バイアス、非対称な失敗方向、大振幅域での歪み)をほぼ全て同時に
解消した。

### M15: 真の系にクーロン摩擦を追加した公平な再検証(#31)
M14の留保を解消するため、真の系に**グレーボックスが既知として持たないクーロン摩擦**
(乾摩擦、`-c_coulomb·sign(θ̇)`、θ̇=0で不連続)を追加して再検証。

| 指標 | ブラックボックス | グレーボックス |
|---|---|---|
| 開ループRMSE (ゼロトルク / ランダム / 共振) | 3.43 / 2.53 / 2.81 | **0.57 / 0.20 / 0.26** |
| M5グリッドで真値の成功/失敗パターンと一致 | 14/18 | **18/18(完全一致)** |
| M12スイープで真値パターンと一致 | 15/15 | 15/15 |
| MPC安定化誤差 平均(オラクル=0.0699) | — | **0.0518** |

**本当の未知項(しかも不連続)があってもグレーボックスの優位性は維持された**。NNは
クーロン摩擦のステップ状の不連続までほぼ正確に再現しており、既知構造を埋め込むことで
NNが「難しい部分だけ」に集中できるという狙いが機能している。ただしグレーボックスの
誤差自体は悪化しており(ゼロトルク条件で0.17→0.57)、未知項が難しくなった分の
コストは確かに存在する。

### M16: パラメータ誤差(キャリブレーション誤差)下での再検証(#33)
M15は「知らない項がある」状況だった。M16ではより厄介な**キャリブレーション誤差**を
検証: 真の系を `L=1.05, m=0.95` にする一方、グレーボックスとPIDは公称値
`L=1.0, m=1.0` を信じたまま(実機で必ず生じる状況)。

| 指標 | ブラックボックス | グレーボックス |
|---|---|---|
| 開ループRMSE (ゼロトルク / ランダム / 共振) | 5.05 / 4.18 / 4.31 | **0.52 / 0.38 / 0.32** |
| M12スイープで真値の成功/失敗パターンと一致 | 5/15 | **15/15(完全一致)** |
| M5グリッドで真値の成功/失敗パターンと一致 | 12/18 | **18/18(完全一致)** |
| MPC安定化誤差 平均(オラクル=0.0218) | — | **0.0342** |

**予想に反して、パラメータ誤差はグレーボックスの弱点にならなかった。** 残差NNが
パラメータ誤差の補正を正しく吸収しており、学習された残差は「真の摩擦 +
(g/L_nom - g/L_true)sinθ + トルク項の誤差」という理論通りの形になっていた(θ=1.5,
τ=3でのオフセット +0.33 は、重力項の誤差 +0.466 とトルク項の誤差 -0.136 の和に一致)。
一方**ブラックボックスはこの条件でM5/M12で見た楽観バイアスが顕著に再発**し、真値が
失敗する条件でも全て成功してしまう(M12スイープ15/15成功、真値は5/15)。

副次的な発見: パラメータ誤差でトルク余裕が約5%減っただけで、MPCの `W_CONTROL=0.01`
では「何もしない」方がコスト最小になり、オラクルMPCすらスイングアップしなくなった
(M4で最初に踏んだのと同じ失敗)。重みを0.001に下げて回復させた。

**教訓**: 既知構造の埋め込みは、未知項が難しくなっても(M15)、既知項が多少
間違っていても(M16)有効。M14の優位性は「未知項が自明に簡単だったから」ではなく、
構造そのものの効果だと確認できた。またサンプリングベースMPCのコスト重み設計は
プラントの変化に対して脆く、制御器のチューニングもプラント変化に対する検証対象に
含めるべきという教訓も得られた。

## 横断的な教訓まとめ

1. **IC/データ分布の偏りは、サロゲートの弱点をそのまま生み出す**(M1→M2)。層化
   サンプリングで対象領域を増やすと改善するが、副作用で別領域にバイアスが移りうる。
2. **制御入力の追加はタスク難度を上げる**(M3)。モデル容量を増やしても自動的には
   改善しない。データ・学習目的の設計の方が効きやすい。
3. **長時間の完全開ループロールアウト予測は本質的に信頼できない**(M1, M3)。実用上は
   短ホライズン予測+頻繁な再計画(MPC)が妥当という設計方針は、M4で実証された。
4. **サロゲートの系統バイアスは複数の制御タスクを横断して一貫した方向性を持ちうる**
   (M4→M6)。一方、訓練データ範囲外では「実際より安定化しやすい」という楽観バイアスも
   ある(M5)ため、範囲外への外挿は特に注意が必要。
5. **マルチステップ損失はカリキュラム学習なしでは不安定化する**(M7)。素朴な実装は
   むしろ悪化させうる。
6. **ロールアウト精度指標(RMSE)の改善は、制御タスクでの実用性を保証しない**
   (M7→M8, M9)。学習目的をどう工夫しても、最終的に使うタスクで直接検証しない限り
   実用上の性能は分からない。これがM1-M13を通じた最大の教訓("objective mismatch
   problem" の具体的な実例)。
7. **「制御タスクの成功率が高い」ことも、良いサロゲートの証明にはならない**(M12)。
   1-stepモデルはトルク飽和で真値が失敗する領域でも常に成功してしまう楽観バイアスを
   持っており、これは現実の制約を捉え損ねているという「誤り」である。評価すべきは
   成功率そのものではなく、**真値の成功/失敗パターンとの一致度**。
8. **1回のオンポリシーデータ収集(DAgger)では、狙った特定の失敗事例を確実に直す
   には不十分**(M13)。反復的な収集→再学習が必要になりうる。
9. **学習方法・データの工夫より、モデル構造(既知物理の埋め込み)の方が桁違いに効く**
   (M14)。M1-M13で積み上げた学習方法・データ側の工夫による改善はいずれも数十%程度
   だったが、既知の物理構造をハードコードしNNに残差のみを学習させるグレーボックス化は、
   一度に桁違いの改善をもたらし、それまでの主要な失敗モードをほぼ全て同時に解消した。
10. **既知構造の埋め込みは、未知項が難しくなっても(M15: 不連続なクーロン摩擦)、
    既知項が多少間違っていても(M16: 5%のパラメータ誤差)有効**。グレーボックスの
    優位性は「真の方程式が完全既知」という特殊な有利さの産物ではなく、構造そのものの
    効果だと確認できた。
11. **サンプリングベースMPCのコスト重み設計は、プラントの変化に対して脆い**(M16)。
    パラメータ誤差でトルク余裕が5%減っただけで、オラクルMPCすら「何もしない」のが
    最適になってしまった。制御器のチューニングもプラント変化に対する検証対象に
    含めるべき。

## 今後の課題

- アンサンブル+不確実性推定により「サロゲートが外挿している領域」を検出できるようにする
  (実機適用時の安全マージン設定に必要)。
- アクチュエータ遅れのような、状態に現れない内部ダイナミクスがある場合の検証。
- DAgger(M13)の反復適用による改善余地の検証。
