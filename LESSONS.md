# M1-M9で得られた教訓

振り子の物理モデルをNeural State Space (NSS) モデルでサロゲート化し、ロールアウト誤差・
制御タスクでの実用性を検証してきたプロジェクトの、マイルストーンごとの要約と横断的な教訓。
各マイルストーンの詳細は対応するGitHub Issueを参照。

## マイルストーン要約

### M1: 単振り子ロールアウト誤差検証(#1)
単振り子(無減衰・無入力)でNSS状態遷移モデル(MLP、`(sinθ, cosθ, θ̇)`エンコード)を
1-step教師強制損失で学習。ロールアウト誤差は学習ホライズン(1秒)を過ぎると加速度的に
蓄積し、約4秒で飽和。サロゲートはエネルギーを単調に注入し続ける非保存的な誤差を持ち、
倒立近傍・完全回転域(高エネルギー域)は一様IC分布では過小表現され、ロールアウトが
大きく破綻することが判明した。

### M2: 減衰項とIC分布の層化(#3)
減衰項 `c` を追加し、IC分布を3層(normal / rotation / inverted)に層化サンプリング。
M1最大の弱点だった倒立近傍・完全回転域の精度が大幅改善(最終RMSE 2.68→2.37)。
一方、完全回転域で「2周目以降の減衰が速すぎる」・小振幅域で「減衰不足でわずかに発散する」
という逆方向のバイアスが両方観測された。

**教訓**: IC分布の偏りは、サロゲートの精度が悪い領域を直接生み出す。層化サンプリングで
対象領域を明示的に増やすことは効果的だが、副作用として別領域にバイアスが移る可能性がある。

### M3: トルク入力の導入(#5)
状態遷移にトルク入力 `τ`(ZOH)を追加し、`x_{t+1}=f_θ(x_t,u_t)` を学習。同一モデル容量では
制御入力を加えたことでタスク難度が上がり、長時間ロールアウトのRMSEはM2単体条件より悪化した
(2.37→3.5〜3.8)。モデル容量を2倍にしても改善せず。一方、エネルギー推移や共振トルクによる
スイングアップの定性的挙動は良く再現できた。

**教訓**: ポイントワイズなロールアウト誤差(RMSE)の悪化は、位相・タイミングのズレによる
ものが大きく、定性的な物理挙動の再現度とは別の話。「長時間の完全開ループ予測より、
短ホライズン予測+頻繁な再計画(MPC)の方が実用的」という設計方針がここで得られた。

### M4: スイングアップMPC(#7)
正弦波パラメータ化したランダムシューティングMPCを実装。実プラントは常に真の物理モデルで
ステップし、予測モデル(NSSサロゲート/真の物理モデル)だけを差し替えて比較。オラクルMPCは
ほぼ完全にθ=πへ収束(誤差0.0065rad)。NSSサロゲートMPCもスイングアップ・大まかな安定化は
達成するが、系統的なオフセット(平均角距離0.27rad、5試行ともstd0.006の低分散=再現性のある
バイアス)が残った。フィードバックなしのベースライン(開ループ共振トルク)は倒立近傍に
全く留まれず、MPC(receding horizon)の効果が明確だった。

**教訓**: M3の設計方針(短ホライズン+頻繁な再計画)は実際に機能し、サロゲートでもMPCの
枠組みに組み込めば実用的なスイングアップ制御ができる。ただし系統バイアスは残る。

### M5: PID安定化(#9)
倒立目標のPIDコントローラを実装し、同一ゲインを真の物理モデル/NSSサロゲートに閉ループ
適用して比較。小さな偏差では両者とも良く一致して安定化する(成功: 真値12/18、サロゲート
15/18)が、訓練データがカバーする範囲の境界〜外側の大きな偏差(角速度±1rad/s以上)では、
**真の物理モデルはトルク飽和により安定化に失敗する一方、サロゲートは誤って成功して
しまう**という逆方向の乖離が見つかった。

**教訓**: サロゲートは訓練データの範囲外で「実際より安定化しやすい」という楽観的な
バイアスを持ちうる。サロゲート単体の評価では気づけず、真値との比較で初めて判明した。

### M6: PTP制御(#11)
PIDを目標角度可変に一般化(重力フィードフォワード `mgL*sin(target)` を追加。倒立(pi)
以外の目標では、これがないと定常重力トルクを補償できず転落するバグを発見・修正)。
倒立近傍の小さなPTP(±0.2rad程度)は真値・サロゲートともに良好に追従する一方、単発
ステップ応答では**オフセット方向によって非対称な失敗パターン**が見られた(真値: 対称的に
±0.4radで失敗。サロゲート: +0.3rad以降で失敗するが-0.4radはむしろ成功)。この非対称性は
M4で見つかった系統的オフセットバイアスと方向的に整合していた。

**教訓**: サロゲートの系統バイアスは複数の制御タスク(MPC・PID・PTP)を横断して一貫した
方向性を持って現れることがあり、一つのタスクで見つかったバイアスが他のタスクの挙動を
予測する手がかりになる。

### M7: マルチステップ(ロールアウト)損失(#15)
1-step教師強制損失を、Kステップ自己回帰展開してから損失を取る「マルチステップ損失」に
変更。**いきなり大きなK(K=10や30)で最初から学習すると、学習損失は下がってもロールアウト
評価(RMSE)はむしろ1-step版より悪化する**ことが判明。K=1→3→5→10→20→30と段階的に
伸ばすカリキュラム学習(+勾配クリッピング)を導入して初めて安定して学習できた。

M3と同じ3条件評価でBefore/After比較した結果は**混合**: ゼロトルク(3.83→3.43)・共振
トルク(3.52→3.39)は改善したが、ランダムトルク(3.64→4.92)は悪化。系統バイアスの
「向き」自体も状態領域によって一貫せず、より複雑な誤差構造になった。

**教訓**: マルチステップ損失は「1-step損失とロールアウト時挙動のミスマッチ」を狙った
改善だが、(1) 素朴な実装では訓練が不安定化しうる(カリキュラム学習が実質必須)、
(2) 改善は均一ではなく条件によってトレードオフがある、という2点が重要な知見。

### M8: M7モデルでのM5(PID)再検証(#17)
M7でリトレインしたモデルに対し、コード変更なしでM5と同じPID安定化評価を再実行。
成功数が真値12/18(不変)・サロゲート15/18→13/18に低下。M5では成功していた
`Δθ0=-0.1, θ̇0=-1.0` がM7モデルでは大きく失敗する**新規のリグレッション**が見つかった
一方、他の複数ICでは逆に改善していた。

### M9: M7モデルでのM4(MPC)・M6(PTP)再検証(#19)
同様にM4・M6もM7モデルで再検証。
- **M4**: NSSサロゲートMPCの平均安定化誤差が0.269→0.541に悪化しただけでなく、
  ばらつきがstd0.006→0.442に激増(試行によって0.06〜1.33と大きくばらつく不安定な挙動)。
- **M6**: 単発ステップ応答の失敗方向が完全に反転(+0.3/+0.4側の失敗→-0.3/-0.4側の失敗)。

**教訓(M8・M9共通、プロジェクト最大の発見)**: **ロールアウト誤差(RMSE)という
集計指標の改善は、個別の制御タスクの成功率・安定性の改善を全く保証しない。**
M4では平均・ばらつき両方が悪化、M5では成功数が悪化、M6では失敗方向が反転する
(かつ一部条件では改善する)という、タスクごとに異なる形で表れた。これは
model-based RL文献でいう "objective mismatch problem" の具体的な実例と言える。

## 横断的な教訓まとめ

1. **IC/データ分布の偏りは、サロゲートの弱点をそのまま生み出す**(M1→M2)。層化
   サンプリングで対象領域を増やすと改善するが、副作用で別領域にバイアスが移りうる。
2. **制御入力の追加はタスク難度を上げる**(M3)。モデル容量を増やしても自動的には
   改善しない。データ・学習目的の設計の方が効きやすい。
3. **長時間の完全開ループロールアウト予測は本質的に信頼できない**(M1, M3)。実用上は
   短ホライズン予測+頻繁な再計画(MPC)が妥当という設計方針は、M4で実証された。
4. **サロゲートの系統バイアスは複数の制御タスクを横断して一貫した方向性を持ちうる**
   (M4→M6)。一方、訓練データ範囲外では「実際より安定化しやすい」という楽観バイアスも
   ある(M5)ため、範囲外への外挿は特に注意が必要。
5. **マルチステップ損失はカリキュラム学習なしでは不安定化する**(M7)。素朴な実装は
   むしろ悪化させうる。
6. **ロールアウト精度指標(RMSE)の改善は、制御タスクでの実用性を保証しない**
   (M7→M8, M9)。学習目的をどう工夫しても、最終的に使うタスクで直接検証しない限り
   実用上の性能は分からない。これがプロジェクト全体を通じた最大の教訓。

## 今後の課題

- M7のマルチステップ損失が制御タスクでリグレッションを起こした根本原因の調査
  (訓練データのトルクホールド長と評価時のトルクパターンの不一致、K-step内での
  重み付け、カリキュラムスケジュールの妥当性など)。
- M3の1-stepモデルに戻すか、M7を改良するか、あるいは別のアプローチ(物理構造を
  組み込んだアーキテクチャ、アンサンブル+不確実性推定など)を検討するかの方針決定。
